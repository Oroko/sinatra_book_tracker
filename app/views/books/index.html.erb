<% if Helpers.logged_in?(session) %>
      <h1>Welcome <%= Helpers.current_user(session).username %></h1>
      
      <a href='/logout'>Log Out</a>
    <% else %>
      <h1>You Must <a href="/">Log In</a> Account </h1>
  <%end%>


